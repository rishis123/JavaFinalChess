public makeMove(String move) { // assume input is in the chess notation, determine which class to put this method in later

  for (int i = 0; i<possibleMoves.size(); i++) {
    if (move.equals(possibleMoves.get(i)) {
      return true;       // want to print inputted method to console
      
    }
  }
  return "Invalid move, please retry";
 }


  
